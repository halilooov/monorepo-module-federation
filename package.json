{
  "name": "monorepo-module-federation",
  "version": "1.0.0",
  "scripts": {
    "start:services": "npm run start -w admin && npm run start -w shop && npm run start -w host"
  },
  "workspaces": [
    "packages/*",
    "services/*"
  ]
}
